name:  Configuration ansible.cfg 

on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u jyrel bash -c 'cat > /home/jyrel/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/jyrel/ansible/inventory.yaml
          roles_path    = /home/jyrel/ansible/roles
          host_key_checking = False
          remote_user = jyrel
          private_key_file = /home/jyrel/.ssh/id_rsa
          log_path = /home/jyrel/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/jyrel/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/jyrel/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown jyrel:jyrel /home/jyrel/ansible/ansible.cfg
          sudo chmod 644 /home/jyrel/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u jyrel ls -la /home/jyrel/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u jyrel cat /home/jyrel/ansible/ansible.cfg
